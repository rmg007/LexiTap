# Next-session prompt (LexiTap)

Paste this to start a fresh session after `/clear`.

---

Read `CLAUDE.md` + `AGENTS.md` first (they are the contract). Then read the work
queue: GitHub Issues, else the task-level plan `plans/RELEASE_PLAN.md`
(authoritative ordering) → roadmap mirror `ROADMAP.md` (root) → canonical
`lexitap-docs/02-product-definition/ROADMAP.md`.

## Current ground truth (verified 2026-05-31)

- App is **already built**, do NOT scaffold from scratch. Clean/hexagonal layers
  under `mobile/src/` (`domain/`, `application/`, `infrastructure/`,
  `presentation/`) + expo-router routes under `mobile/app/`. ~120 TS files, tests
  colocated. DI via `mobile/src/composition/container.ts` — **not** Redux/Zustand,
  **not** TanStack Query.
- Stack: Expo + expo-router + expo-sqlite + nativewind + react-native-paper +
  @supabase/supabase-js. (No zustand / react-query / react-native-purchases.)
- IAP: `StubIapService` only; RevenueCat deferred to Phase 3 (see AGENTS.md).
- **Monetization model changed 2026-05-31** → one-time exam packs ($9.99) +
  All-Exams bundle ($29.99), NO subscriptions, B2B deferred. Authoritative:
  `lexitap-docs/08-financial-legal/REVENUE_MODEL_PRICING.md` +
  `memory/2026-05-31_monetization_rethink.md`. `tiers.ts` + `words.db` still
  encode the dead subscription model — rebuild needed (see below).

## The real blocker to launch: content VOLUME

- Delivery is **solved in code** (task C0, merged 2026-05-31): `words.db` is
  bundled at `mobile/assets/vocab/words.db`, embedded via the `expo-asset` plugin
  + `metro.config.js` `.db` asset ext, and copied into expo-sqlite's dir at launch
  by `infrastructure/db/contentDb.ts` (version-gated, `BUNDLED_CONTENT_DB_VERSION`)
  before the ATTACH in `database.ts`. ⚠️ **Still unverified on a physical device** —
  the unit tests use fakes; nothing proves the real asset→copy→ATTACH path. Verify
  on device before trusting "content loads."
- **Volume is the long pole:** the bundled db holds only **~216 words** (~7%), not
  a launch-ready catalog. Enriching + rebuilding `words.db` via `content-tool` is
  the critical path (task C1). See `plans/RELEASE_PLAN.md`.

## Monetization rebuild needed (model changed 2026-05-31)

Both `tiers.ts` AND `words.db` encode dead models and must be rebuilt to the new
one-time exam-pack model (`REVENUE_MODEL_PRICING.md`). Two coupled tasks:

1. **Schema → many-to-many.** `words.tier_id` single-FK → `word_tiers` junction
   (a word carries multiple free category tags + exam membership). Word IDs become
   word-text-only (`word_${hash}`), so one word = one progress row across
   categories. Touches `content-tool` schema/import/export/validate + `mobile`
   db queries/repositories/mappers/rows. (Started + reverted this session — design
   was entangled with pricing; pricing now settled, so it can proceed.)
2. **Products → exam packs.** `tiers.ts` rewrite: drop subscription SKUs
   (`premium.monthly/.annual`) + standalone `common3k`; add exam-pack products
   ($9.99) + `all_exams` bundle ($29.99) + upgrade SKUs; free categories carry no
   product. Then rebuild `words.db` from `content-tool` so `content_tiers` matches.

## Also unwired (deferred, not bugs)

- Auth + encrypted user.db blob backup → Phase 3/5.
- EAS build config not yet in `mobile/app.json`.

Pick ONE task per session. Commit + push before `/clear`.
