// Re-exports from the canonical location. Import from @/test-utils/renderWithProviders directly.
export {
  TIER,
  BATCH,
  makeWord,
  makeSession,
  defaultServices,
  renderWithProviders,
  assertNoTextInput,
} from '@/test-utils/renderWithProviders';
