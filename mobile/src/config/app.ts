// App identity. The domain/application layers are parameterized by this id so
// a future sister app reuses them unchanged (SYSTEM_ARCHITECTURE.md). Only
// presentation/ and config/ are rewritten per app.

export const APP_ID = 'lexitap';
