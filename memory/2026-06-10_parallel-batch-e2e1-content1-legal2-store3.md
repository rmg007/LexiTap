# Session: Parallel batch — E2E-1 + CONTENT-1 + LEGAL-2 + STORE-3 (2026-06-10)

4 tasks run concurrently in isolated git worktrees via Workflow, merged cleanly (octopus, no conflicts). 474 mobile tests / 165 content-tool tests — all green after merge.

## E2E-1 · Maestro learn-loop flow ✅
`mobile/.maestro/learn-loop.yaml` — taps through age-gate → onboarding → learn batch (repeat:10, guarded on "Got it" visibility) → asserts "Quick check" header → answers one question (index:0 on any option) → asserts Home. Cannot run green until BUILD-1 (real device/sim build). Weakest step: `tapOn: index: 0, text: ".*"` may need a label-specific tap when running on real device.

## CONTENT-1 · synthesis + pre-write validation ✅
`content-tool/src/commands/synthesize-senses.ts`:
- `validateSenseIngestItem(item)` — 10 invariants (V1–V10): word_id format, non-empty senses, contiguous sense_index, non-empty short_gloss/explanation, explanation ≠ short_gloss, non-empty examples, no cloze blanks in examples, contiguous example_index.
- `serializeSenseIngestFile(items)` — canonical JSONL, deterministic field order, round-trip guarantee.
34 new tests; 165 total. CONTENT-2 now `ready` (unblocked).

## LEGAL-2 · 16+ age gate ✅
`OnboardingAgeGateScreen`: persistence via AsyncStorage (`lexitap.age.gate.passed` / `lexitap.age.gate.rejected`). Auto-advance on already-passed. Permanent dead-end (no Continue) on already-rejected — renders with `accessibilityRole='alert'`. `AgeGateScreen.render.test.tsx`: 5 tests. Routing was already correct (age is first onboarding step). Blocking logic was already correct; persistence was the only gap.

## STORE-3 · expo-doctor audit ✅
16/18 checks pass. Two findings:
1. **metro@0.84.4** — KNOWN-BENIGN (nativewind side-effect; cannot fix without SDK bump; SDK bump forbidden mid-cycle).
2. **`.expo/` tracked in git** — FIXED: `git rm --cached mobile/.expo/types/router.d.ts` + added `.expo/` to `mobile/.gitignore`. `router.d.ts` is auto-generated by Expo Router, machine-local, harmless but unnecessary.
No SDK bump warranted before launch.
